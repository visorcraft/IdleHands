name: publish-npm

on:
  release:
    types: [published]
  workflow_dispatch:

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest
    environment: npm
    env:
      NPM_CONFIG_AUDIT: "false"
      NPM_CONFIG_FUND: "false"
      IDLEHANDS_QUIET_WARNINGS: "1"
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          registry-url: 'https://registry.npmjs.org'

      - name: Install pnpm
        uses: pnpm/action-setup@v4

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile --config.auto-install-peers=false

      - name: Ensure A2UI prebuilt bundle fallback
        run: |
          mkdir -p src/canvas-host/a2ui
          if [ ! -f src/canvas-host/a2ui/a2ui.bundle.js ]; then
            echo "// A2UI fallback bundle for CI release builds" > src/canvas-host/a2ui/a2ui.bundle.js
          fi

      - name: Build
        run: pnpm build

      - name: Verify package contents
        run: pnpm pack --dry-run

      - name: Publish to npm (Trusted Publishing / OIDC)
        run: pnpm publish --access public --no-git-checks
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
