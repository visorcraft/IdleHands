{
  "name": "bench-noisy-build",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=24" },
  "scripts": {
    "build": "node scripts/build.js",
    "test": "node --test"
  }
}
