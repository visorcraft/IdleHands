{
  "name": "portal_realrepo_build_compare",
  "engine": "both",
  "workspace": { "kind": "fixed", "dir": "/home/user/projects/examplecorp/portal" },
  "setup": [
    "git status -s >/dev/null 2>&1 || true",
    "rm -rf public/build || true"
  ],
  "instruction": "In this repo: run `npm run build` and verify it succeeds. If it fails, fix the cause with minimal changes. Respond with exactly: OK",
  "success": { "type": "exec", "command": "npm run build", "exitCode": 0 },
  "repetitions": 5,
  "max_tokens": 1024,
  "model": "Qwen3-Coder-Next-Q4_K_M.gguf"
}
