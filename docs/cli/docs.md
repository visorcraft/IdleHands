---
summary: "CLI reference for `idlehands docs` (search the live docs index)"
read_when:
  - You want to search the live IdleHands docs from the terminal
title: "docs"
---

# `idlehands docs`

Search the live docs index.

```bash
idlehands docs browser extension
idlehands docs sandbox allowHostControl
```
