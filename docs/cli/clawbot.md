---
summary: "CLI reference for `idlehands clawbot` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `idlehands clawbot ...`
  - You need migration guidance to current commands
title: "clawbot"
---

# `idlehands clawbot`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `idlehands clawbot qr` (same behavior as [`idlehands qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `idlehands clawbot qr` -> `idlehands qr`
